program demo;
var int: i, j, k;
float: f;
string: s;
bool: b;
char: c;


function void demo_void()
{
    print("void sin parametros y sin variables locales");
}

function void demo_void_with_vars(int l, int m)
var float: f;
{
    print("void con parametros y variables");
}

function int fibonacci(int o)
{
   if (o<0) then {
      print("Incorrect input");
      return (-1);
   }
   if (o==1) then {
      return (0);
    }
   if (o==2) then {
      return (1);
   }
   return (fibonacci(o-1)+fibonacci(o-2));
}

// esto es un comentario de linea

main()
{
    /* esto es un comentario
        de muchas
        lineas
    */

    input(j);
    i = fibonacci(j);

    print("VALOR DE FIBONNACI INGRESADO: ", i);

    if (i > 10) then {
        // hace algoqui
    }
    else {
        // hace otra cosa
    }

    while(i > 0) do {
        i = i-1;
    }
    // i tiene valor 0 aqui
}